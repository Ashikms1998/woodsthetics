<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Offer</title>
  <!-- Add Tailwind CSS CDN link here -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/5.0.0/normalize.min.css">
  <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/cropper/2.3.4/cropper.min.css'>
  
</head>

<body class="bg-gray-100">
  <div class="container mx-auto p-10">
    <h1 class="text-2xl font-semibold mb-4">Edit Offer</h1>

    <form class="mb-0 space-y-2" action="/editoffer/<%=selectedOffer._id%>" method="post" ">

      <!-- Form fields go here -->
       
        <label for="discount" class=" form-label block text-sm font-medium text-gray-600">Discount:</label>
        <input type="number" id="discount" name="discount" class="form-control mt-1 p-2 border border-gray-300 rounded w-full" value="<%= locals.selectedOffer.discount %>" required>
        
  
        <label for="count" class="form-label block text-sm font-medium text-gray-600">Count:</label>
        <input type="number" id="count" name="count" class="form-control mt-1 p-2 border border-gray-300 rounded w-full" value="<%= locals.selectedOffer.count %>" required>
      
        <label for="expiryDate" class="form-label block text-sm font-medium mt-3 text-gray-600">Expiry Date:</label>
        <input type="date" id="expiryDate" name="expiryDate" class="form-control mt-1 p-2 border border-gray-300 rounded w-full" value="<%=locals.selectedOffer.expiryDate %>">
  
        <div class="invisible">
        <input type="text" value="<%= locals.selectedOffer._id %>" class="" id="id" name="id">
    </div>

    <div class="flex items-center justify-center">
    <button type="submit" class="btn bg-red-500 font-bold py-2 px-4 rounded">Update Offer</button>
    </div>
    </form>
  </div>

</body>

</html>